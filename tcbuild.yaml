input:
  easy-installer:
    toradex-feed:
      version: "5.7.2"
      release: quarterly
      machine: colibri-imx8x
      distro: torizon
      variant: torizon-core-docker
      build-number: "20"

output:
  easy-installer:
    local: image_custom
    bundle:
      compose-file: docker-compose.yml
